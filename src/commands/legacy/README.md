# Legacy Command Implementations

This folder contains command implementations that have been superseded by refactored or SOLID-compliant versions. They are preserved here for reference and historical purposes.

If you need to restore any command, move it back to the main `src/commands/` directory and update the command factory registration.
